<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="item"
            type="String" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:background="@drawable/bg_radio_button"
        android:backgroundTint="@color/green_E6E4D7">

        <TextView
            android:id="@+id/option_title"
            style="@style/ProductTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="4dp"
            android:text="@{item}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="亞麻杏色上衣-M" />

        <ImageButton
            android:id="@+id/text_cart_remove"
            android:layout_width="15dp"
            android:layout_height="15dp"
            android:layout_marginStart="32dp"
            android:layout_marginEnd="8dp"
            android:backgroundTint="@color/transparent"
            android:src="@drawable/ic_baseline_close_24"
            android:tint="@color/gray_646464"
            app:layout_constraintBottom_toBottomOf="@+id/option_title"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/option_title"
            app:layout_constraintTop_toTopOf="@+id/option_title" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>